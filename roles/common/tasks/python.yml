- name: Install python3 packages
  package:
      name: "{{ item }}"
      state: installed
  loop:
    - python3 
    - gcc 
    - python3-devel

- name: install dependencies
  package:
    requirements: requirements.txt
    executable: pip3.9
  args:
    chdir: /app
