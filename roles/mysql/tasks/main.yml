- name : Install Mysql server
  package:
      name: mysql-server
      state: present

- name: Enbale & Start mysql service
  service:
      name: mysqld
      state: started
      enabled: yes
  
- name: Setup root password
  command:
    mysql_secure_installation --set-root-pass RoboShop@1