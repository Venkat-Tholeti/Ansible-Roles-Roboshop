---

# we can write as below as well by passing variable from command line as argument

- name: Configurig "{{ component }}"
  #hosts: "{{ component }}"
  hosts : localhost
  connection : local
  become: yes
  roles: #This will automatically load a folder called roles in current working directory
   - "{{ component }}"
    
    